__pycache__/
*.pyc
.venv/
venv/

# runtime artifacts
evidence/*.jpg
uploads/*
!uploads/.gitkeep
zones.json

# model weights (large; fetch/train separately)
*.pt
!models/.gitkeep

# sample footage (large; not committed)
sample_data/*
!sample_data/.gitkeep

.DS_Store

# capture sessions + fine-tune frames (large, local only)
data/

# OpenCLIP weights (578 MB), downloaded once into the cache
models/clip/

# weapon alert debug dumps
debug/
