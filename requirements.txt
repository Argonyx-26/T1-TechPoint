fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
opencv-python-headless==4.10.0.84
numpy==1.26.4
ultralytics==8.3.40
pydantic==2.9.2
lapx>=0.5.2   # ByteTrack association (ultralytics tracker); without it every frame errors
open_clip_torch==3.3.0   # Ask Vigil search + re-ID (weights cached in models/clip)
segno==1.6.6            # QR codes for the phone-GPS locate links
cryptography==50.0.1    # self-signed HTTPS cert for the locate page (phones give GPS only to HTTPS)
